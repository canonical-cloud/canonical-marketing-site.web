{
  "name": "canonical-marketing-site.web",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npm run build && node --test tests/site-contract.test.mjs tests/csp-contract.test.mjs tests/actions-contract.test.mjs",
    "test:browser": "npm run build && node --test --test-concurrency=1 --test-timeout=120000 tests/*-playwright.test.mjs tests/*-puppeteer.test.mjs"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "puppeteer": "^25.3.0"
  }
}
