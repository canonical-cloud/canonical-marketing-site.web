{
  "name": "canonical-cloud-frontend",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/site-contract.test.mjs",
    "test:browser": "npm run build && node --test --test-concurrency=1 --test-timeout=120000 tests/site-playwright.test.mjs tests/site-puppeteer.test.mjs"
  },
  "dependencies": {
    "astro": "^7.0.7"
  },
  "devDependencies": {
    "playwright": "^1.56.0",
    "puppeteer": "^25.3.0"
  }
}
