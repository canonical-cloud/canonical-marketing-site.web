name: Agent instruction hierarchy

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: canonical-cloud/canonical.cloud/.github/workflows/agents-hierarchy.yml@b0307b4d794e2f525937f9cc9f5d143a855e769d
