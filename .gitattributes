# Normalize line endings; keep shell scripts LF so they run on Linux CI.
* text=auto eol=lf
*.sh text eol=lf
*.mjs text eol=lf

# sops ciphertext is line-oriented dotenv; keep it LF so the MAC stays valid.
/env/enc/*.env.enc text eol=lf
